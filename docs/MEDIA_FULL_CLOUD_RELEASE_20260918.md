# Media全機能のクラウド反映（2026-09-18）

ローカルで作成したMediaを一部だけ公開した状態を修正する。独立したログイン中の実名表示を廃止し、共通メニューにmikke IDとログアウトを渡す。実名・メールへのフォールバックはしない。

## 対象

- 共通エディターの全ブロック、文字装飾、部分リンク、画像リンク、YouTube、複合画像、リンクカード
- Mediaと発信者の設定、非公開画像保存、複数カテゴリー、公開順、上部固定、特集・シリーズ
- 読者ページの検索・カテゴリー・月・特集絞り込みとページ分割
- マイMedia、保存、お気に入り、フォロー、任意プロフィール、返信通知
- コメントと返信、承認、ありがとう、通報、運営非表示の保持
- 閲覧・共有と保存・お気に入りの集計

Academy等の既存利用側では画像URLコールバックの互換性を維持する。利用者の記事公開や同意操作は実施していない。

## 保存先

正本は `supabase/migrations/20260918131908_media_full_release.sql`。
SHA256: `DB3AC069E44A50D5773EDCFD75DB4BAA135317DD25C92B0E0A08E315CD4E5483`。
本番DBの適用記録は `20260918135002 / media_full_release`。再適用しない。
適用前後の既存データ件数はサイト1、記事1、公開版0で一致した。
既存記事・公開版へのバックフィルUPDATEは行わない。旧カテゴリーは読込時に補完する。

## 確認済み

- 本番ビルド成功（webpack、TypeScript、182ページ生成）。既存のthemeColor警告あり。
- lint、差分空白チェック
- PostgreSQL 17.6の隔離環境で正本移行を検証。全ブロック公開、設定、57記事のページ分割、読者権限、既存データ不変、既存非公開制約、公開DTO、ロールバック残留0が成功
- エディター、表示DTO、セッション切替、リンク情報取得、計測APIの個別テスト
- 本番DBの新規テーブルRLSと、匿名・認証済みからの直接書込や計測RPCの権限制限を確認

## 配信時に必要な設定と未確認項目

Netlifyの管理画面が未ログインのため、本番配信ブランチと自動公開の設定、次の変数の有無を確認する必要がある。値をログや文書へ出さない。

- NEXT_PUBLIC_MEDIA_FREE_ENABLED=true
- NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY
- MEDIA_PRIVATE_ASSETS_ENABLED=true
- MEDIA_PUBLIC_DATABASE_ENABLED=true
- MEDIA_PRIVATE_ASSET_BUCKET=mikke-media-private
- SUPABASE_SECRET_KEY または SUPABASE_SERVICE_ROLE_KEY（サーバー限定）

Netlifyの旧配信が残っていたため、GitHubマージやVercel Readyだけでは完了扱いにしない。実配信URLのコード更新、390px/1280pxの目視、実ログインでの保存・画像・読者画面確認は別途必要。ブラウザー接続不調により、この記録時点では全機能版の目視確認は未完了。

計測は信頼する配信環境の接続元ヘッダーが得られない場合は保存しない。生IPは保存せず日別ハッシュと接続別・全体上限を使う。Netlifyは公式の制限用途推奨を確認したが、入力された同名ヘッダーの上書き保証の厳密文言は未確認。
